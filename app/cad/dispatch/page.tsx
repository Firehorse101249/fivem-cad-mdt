import { UnderConstruction } from "../../_components/UnderConstruction";

export default function DispatchPage() {
  return (
    <UnderConstruction
      title="Dispatch"
      description="Call intake, priority queue, unit assignment, and scene tracking are reserved for the dispatch workflow."
    />
  );
}
