import { UnderConstruction } from "../../_components/UnderConstruction";

export default function SettingsPage() {
  return (
    <UnderConstruction
      title="Settings"
      description="Department configuration, preferences, identifiers, and integration settings are planned for this page."
    />
  );
}
